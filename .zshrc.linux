
# Color support
if [ "$TERM" = "xterm" ]
then
    export TERM="xterm-256color"
fi

# ls color
alias ls='ls --color=auto'

export PATH=/home/haruki/.nimble/bin:$PATH

export PATH='/home/linuxbrew/.linuxbrew/bin:/home/linuxbrew/.linuxbrew/sbin':"$PATH"
